# Port Contracts

Formal interface contracts for each port category are defined in **Objective 1.5: Abstraction Architecture** and will be populated here once that objective is implemented.

Each contract document will cover:

- The port interface definition (TypeScript)
- Capability matrix (what each adapter supports)
- Conformance test suite location
- Known adapter implementations
