name: Windows Nightly

on:
  schedule:
    # 02:00 UTC every night
    - cron: '0 2 * * *'
  workflow_dispatch:

concurrency:
  group: windows-nightly
  cancel-in-progress: false

jobs:
  windows-full-suite:
    name: Full suite on Windows Server 2022
    runs-on: windows-2022
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc

      - uses: pnpm/action-setup@v4

      - name: Get pnpm store path
        id: pnpm-cache
        run: echo "STORE_PATH=$(pnpm store path --silent)" >> "$env:GITHUB_OUTPUT"
        shell: pwsh

      - name: Cache pnpm store
        uses: actions/cache@v4
        with:
          path: ${{ steps.pnpm-cache.outputs.STORE_PATH }}
          key: windows-pnpm-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: windows-pnpm-

      - name: Cache Turbo
        uses: actions/cache@v4
        with:
          path: .turbo
          key: windows-turbo-${{ github.sha }}
          restore-keys: windows-turbo-

      - run: pnpm install --frozen-lockfile

      - name: Typecheck
        run: pnpm typecheck

      - name: Lint
        run: pnpm lint

      - name: Unit + integration tests
        run: pnpm test
        env:
          CI: 'true'

      - name: Cross-platform tests
        run: pnpm test:cross-platform

      - name: Build
        run: pnpm build

      - name: Audit catalog check
        run: pnpm audit:catalog

      - name: Performance baseline check (Windows)
        # Compares pnpm bench:windows output against bench/baselines/windows-p95-ms.json.
        # Fails if any p95 exceeds the baseline target by more than regression_threshold_pct (20%).
        # Skipped when LOAD_TEST_BASE_URL is not set (no staging environment).
        env:
          LOAD_TEST_BASE_URL: ${{ secrets.LOAD_TEST_BASE_URL }}
          BASELINE_FILE: bench/baselines/windows-p95-ms.json
        run: |
          if (-not $env:LOAD_TEST_BASE_URL) {
            Write-Host "LOAD_TEST_BASE_URL not set — skipping Windows perf baseline check"
            exit 0
          }
          pnpm bench:windows --baseline $env:BASELINE_FILE
        shell: pwsh

  windows-2019-compat:
    name: Compatibility check on Windows Server 2019
    runs-on: windows-2019
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc

      - uses: pnpm/action-setup@v4

      - run: pnpm install --frozen-lockfile

      - name: Cross-platform tests (2019)
        run: pnpm test:cross-platform

      - name: Build (2019)
        run: pnpm build
