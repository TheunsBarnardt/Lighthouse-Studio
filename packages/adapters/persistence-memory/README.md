# @platform/adapter-persistence-memory

In-memory adapter for `@platform/ports-persistence`. Stores entities in Maps.

**Use only for testing and development.** All data is lost when the process stops.
