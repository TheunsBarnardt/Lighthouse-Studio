# @platform/adapter-storage-memory

In-memory object storage adapter for `@platform/ports-storage`. Stores objects as Buffers in a Map.

**Use only for testing and development.** Does not support signed URLs.
