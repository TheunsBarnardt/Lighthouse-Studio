{
  "name": "@platform/adapter-identity-postgres",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/src/index.js",
      "types": "./dist/src/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "lint": "eslint src tests",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@node-rs/argon2": "^2.0.2",
    "@platform/ports-identity": "workspace:*",
    "neverthrow": "8.2.0",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@platform/config": "workspace:*",
    "@types/pg": "^8.11.10",
    "rimraf": "6.0.1",
    "vitest": "3.2.3"
  }
}
