'use client';

import { ExternalLink, Zap, Clock, Link2 } from 'lucide-react';

import type { DocNavItem } from '../page';

const LINKED_SURFACES: Record<string, string[]> = {
  contact: [
    'Schema Designer → Contact entity',
    'API Reference → GET /contacts',
    'ContactsTable component',
  ],
  'api-rest': ['Stage 7: API Generation', 'Deployment: REST endpoints'],
  'contacts-table': ['Stage 6: UI Generation', 'Schema: Contact entity'],
};

export function DocMetaPanel({ page }: { page: DocNavItem }) {
  const linkedSurfaces = LINKED_SURFACES[page.id] ?? [];

  return (
    <div
      style={{
        width: 224,
        flexShrink: 0,
        borderLeft: '1px solid var(--border-default)',
        overflowY: 'auto',
        padding: '16px 16px',
        display: 'flex',
        flexDirection: 'column',
        gap: 20,
      }}
    >
      <div style={{ display: 'flex', flexDirection: 'column', gap: 8 }}>
        <p
          style={{
            fontSize: 11,
            fontWeight: 600,
            color: 'var(--fg-tertiary)',
            textTransform: 'uppercase',
            letterSpacing: '0.06em',
            margin: 0,
          }}
        >
          Page info
        </p>

        <div style={{ display: 'flex', flexDirection: 'column', gap: 6 }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: 6, fontSize: 12 }}>
            <Clock style={{ width: 13, height: 13, color: 'var(--fg-tertiary)' }} />
            <span style={{ color: 'var(--fg-tertiary)' }}>Updated 2h ago</span>
          </div>

          <div style={{ display: 'flex', alignItems: 'center', gap: 6, fontSize: 12 }}>
            <Zap style={{ width: 13, height: 13, color: 'var(--fg-warning)' }} />
            <span style={{ color: 'var(--fg-tertiary)' }}>
              Source:{' '}
              <span style={{ color: 'var(--fg-primary)' }}>
                {page.sourceType.replace('_', ' ')}
              </span>
            </span>
          </div>
        </div>
      </div>

      <div style={{ display: 'flex', flexDirection: 'column', gap: 8 }}>
        <p
          style={{
            fontSize: 11,
            fontWeight: 600,
            color: 'var(--fg-tertiary)',
            textTransform: 'uppercase',
            letterSpacing: '0.06em',
            margin: 0,
          }}
        >
          Status
        </p>
        <span
          className={page.autoGenerated ? 'pg-badge pg-badge-warning' : 'pg-badge pg-badge-default'}
          style={{ display: 'inline-flex', alignItems: 'center', gap: 4, width: 'fit-content' }}
        >
          {page.autoGenerated ? (
            <>
              <Zap style={{ width: 11, height: 11 }} />
              Auto-generated
            </>
          ) : (
            'Human-maintained'
          )}
        </span>
      </div>

      {linkedSurfaces.length > 0 && (
        <div style={{ display: 'flex', flexDirection: 'column', gap: 8 }}>
          <p
            style={{
              fontSize: 11,
              fontWeight: 600,
              color: 'var(--fg-tertiary)',
              textTransform: 'uppercase',
              letterSpacing: '0.06em',
              margin: 0,
              display: 'flex',
              alignItems: 'center',
              gap: 4,
            }}
          >
            <Link2 style={{ width: 11, height: 11 }} />
            Linked surfaces
          </p>
          <ul
            style={{
              listStyle: 'none',
              padding: 0,
              margin: 0,
              display: 'flex',
              flexDirection: 'column',
              gap: 6,
            }}
          >
            {linkedSurfaces.map((s, i) => (
              <li key={i} style={{ display: 'flex', alignItems: 'flex-start', gap: 6 }}>
                <ExternalLink
                  style={{
                    width: 11,
                    height: 11,
                    marginTop: 2,
                    flexShrink: 0,
                    color: 'var(--fg-tertiary)',
                  }}
                />
                <span style={{ fontSize: 12, color: 'var(--fg-tertiary)', lineHeight: 1.4 }}>
                  {s}
                </span>
              </li>
            ))}
          </ul>
        </div>
      )}

      <div style={{ display: 'flex', flexDirection: 'column', gap: 8 }}>
        <p
          style={{
            fontSize: 11,
            fontWeight: 600,
            color: 'var(--fg-tertiary)',
            textTransform: 'uppercase',
            letterSpacing: '0.06em',
            margin: 0,
          }}
        >
          Auto-gen bar
        </p>
        <p style={{ fontSize: 12, color: 'var(--fg-tertiary)', margin: 0 }}>
          {page.autoGenerated
            ? 'This page updates automatically when its source artifact changes.'
            : 'This page is manually maintained and will not auto-update.'}
        </p>
      </div>
    </div>
  );
}
